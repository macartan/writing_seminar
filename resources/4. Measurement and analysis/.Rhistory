rm(list = ls())
setwd("~/Dropbox/Honors_2021/1 Resources and Readings/Materials - Monday October 1")
# The hashtag makes a comment and R will not read it as a command
rm(list = ls()) # This command clears the R environment. Mostly it is useful prior to starting a new session.
## Setting a working directory
setwd("~/Dropbox/Honors_2021/1 Resources and Readings/Materials - Monday October 1") # This is the direction for our class resources today
knitr::opts_chunk$set(echo = TRUE) # Changes the default settings of different chunks of code in our R Markdown file.
### General use packages
install.packages("tidyverse") # This package is actually a suite of packages
library(tidyverse)
### Packages for spatial data
install.packages("raster") # Reading, writing, and manipulating raster form spatial data
install.packages("rgdal") # Provides access to spatial projection operations
install.packages("GISTools") # Choropleth plots and aggregation of point data to polygons
install.packages("rgeos") # Operations with spatial data (i.e. intersection, length of spatial forms)
install.packages("maptools") # More tools for manipulating geographic data
library(raster)
library(rgdal)
library(GISTools)
library(rgeos)
library(maptools)
# Note that you first need to install packages and then save them to your library. Once you have installed them once you then just need to add them to the library for new sessions.
x <- 3 #this assigns an object
x == 3 #this is a logical question
y <- "ABC" #this assigns a character string
x <- c(3,4,5,6) #this is a numeric vector
m <- c("panda", "octopus", "squirrel", "moose") #this is a character vector
panda <- c(x,m) # this combines x and m into one vector
panda <- rbind(x,m) # this binds x and m into a matrix by rows
panda <- cbind(x,m) # this binds x and m into a matrix by columns
panda <- as.data.frame(panda) # this makes y into a data frame
a <- panda[,1] # this subsets y into the first column
b <- panda[1,] # this subsets y into the first row
```{r load conservation data, include = FALSE}
